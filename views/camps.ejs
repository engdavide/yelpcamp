<% include partials/header %>

<h1>This is the campgrounds page</h1>

<a href="/camps/new">Add new campground</a>

<% camps.forEach(function(campground){ %>
    <div>
        <h4><%= campground.name %></h4>
        <img src="<%=campground.image%>"></img>
    </div>
    
    
<% }); %>



<a href=/>Home</a>

<% include partials/footer %>